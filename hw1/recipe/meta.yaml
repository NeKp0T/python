package:
  name: "codetreegen"
  version: "0.1"

# source:
#   git_rev:
#   git_url:

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - astunparse
    - pillow

# test:
#   imports:
#     -

source:
  path: ..

# build:
#   script: python setup.py install

about:
  summary: "Generates nice images of your functions' ASTs"
